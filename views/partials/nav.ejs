<nav class="site-nav">

    <div class="nav-main">
        <a href="/">Home</a>

        <% if (isAuthenticated) { %>

            <a href="/messages/new">New Message</a>

            <a href="/users/profile">Profile</a>

        <% } %>
    </div>


    <div class="nav-auth">

        <% if (isAuthenticated) { %>

            <form action="/logout" method="POST">
                <button class="nav-logout" type="submit">
                    Logout
                </button>
            </form>

        <% } else { %>

            <a class="nav-login" href="/login">
                Login
            </a>

            <a class="nav-signup" href="/users/create">
                Sign Up
            </a>

        <% } %>

    </div>

</nav>

<%- include("flashMessage") %>